<!DOCTYPE html>
<html>
  <head>
    <title>BRANDFUNDING</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% flash.each do |key, value| %><%= content_tag(:div, value, class: "#{key}") %><% end %>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark border-bottom border-white" style="background-color:#cd22dc;" >
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
              <%= link_to 'BRANDFUNDING', root_path, class: "nav-link" %>
            </li>
            <ul class="nav navbar-nav">
              <% if logged_in? %>
                <li><%= link_to "profile", user_path(current_user.id), class: "nav-link" %></li>
                <li><%= link_to "artisan", artisan_profile_path(current_user.id), class: "nav-link" %></li>
                <li><%= link_to "Logout", session_path(current_user.id), method: :delete, class: "nav-link" %></li>
              <% else %>
                <li><%= link_to "Sign up", new_user_path, class: "nav-link"  %></li>
                <li><%= link_to "Login", new_session_path, class: "nav-link"  %></li>
              <% end %>
            </ul>
          </ul>
        </div>
      </nav>
    </header>

    <%= yield %>

    <footer class="footer-1">
      <h2 class="footer-1__title">BRANDFUNDING</h2>
      <p class="footer-1__column">ご利用規約</p>
      <p class="footer-1__column">|</p>
      <p class="footer-1__column">プライバシーポリシー</p>
      <p class="footer-1__column">|</p>
      <p class="footer-1__column">運営会社</p>
    </footer>
  </body>
</html>
