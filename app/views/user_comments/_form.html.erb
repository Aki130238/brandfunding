<%= form_with(model: [user, user_comment]) do |form| %>
  <% if user_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_comment.errors.count, "error") %> prohibited this comment from being saved:</h2>
      <ul>
      <% user_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_content %>
    <%= form.text_area :user_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
